#include <stdio.h>

int sign(float x) // Создаем функцию, удовлетворяющюю условиям задания
{
    if (x < 0) 
     return -1;     
    if (x == 0) 
     return 0;     
    if (x > 0) 
     return 1; 
}
